[project]
name = "gemini-websearch-mcp"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "asyncpg>=0.31",
    "fastmcp>=3.1.1",
    "google-genai>=1.68.0",
]

[dependency-groups]
dev = [
    "pytest-asyncio>=1.3",
]
